# Portfolio

A personal portfolio website built with React, TypeScript, and Sanity CMS.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
